---
title: Reference
description: Lookup shelf — commands, config keys, frontmatter fields, graders, output formats, and state.
---

Exhaustive detail the journey sections link into. Everything here is verified against the source and
the test suite rather than described from memory — flag tables against `src/cli.ts`, config keys
against `src/core/config-schema.json`, output samples captured from real runs.

| Page | Covers |
|---|---|
| [CLI](/docevals/reference/cli/) | All eight commands, every flag, and the exit code each produces. |
| [Configuration](/docevals/reference/configuration/) | Every `docevals.config.yaml` key, its type, and its default. |
| [Frontmatter](/docevals/reference/frontmatter/) | Every eval field, the three entry forms, and how page evals resolve against config evals and suites. |
| [Graders](/docevals/reference/graders/) | Every grader kind and its options, including the ones with sharp edges. |
| [Output and exit codes](/docevals/reference/output-and-exit-codes/) | The `human`, `json`, `markdown`, and `github` shapes; what `0`/`1`/`2` mean and who each routes to. |
| [Files and state](/docevals/reference/files-and-state/) | What docevals writes, where, and which of it belongs in version control. |
| [Glossary](/docevals/reference/glossary/) | The terms that recur across every section. |

## If you are looking for

- **why a build went red** → [Fix a failing eval](/docevals/fix/)
- **what a flag does** → [CLI](/docevals/reference/cli/)
- **what a config key defaults to** → [Configuration](/docevals/reference/configuration/)
- **which grader to reach for** → [Graders](/docevals/reference/graders/), or
  [Deterministic checks](/docevals/evals/deterministic-checks/) for the reasoning
- **whether to commit a file docevals wrote** → [Files and state](/docevals/reference/files-and-state/)

This shelf supports navigation; it does not drive it. If you are trying to accomplish something
rather than look something up, start from a section in the sidebar — the journey pages explain the
path and link here for detail.
