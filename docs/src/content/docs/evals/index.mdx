---
title: Write evals
description: Declare evals in frontmatter, write assertions a grader can decide, and share them across pages.
---

Everything about authoring: the frontmatter contract, what makes an assertion judgeable, and how to
turn per-page assertions into a library the whole corpus shares.

This section is planned and not yet written. It will cover:

- Declaring evals in page frontmatter — array shorthand, object form, `suite`, and `skip`
- Writing assertions a grader can actually decide, using `evidence` and `examples`
- Deterministic checks: `command` evals and the `tool:*` graders that wrap existing linters
- Named evals, suites, and target pass rates
- Severity, and testing the commands a page presents

Until then, see the [README](https://github.com/hawkeyexl/docevals#readme) and the published
frontmatter schema.
